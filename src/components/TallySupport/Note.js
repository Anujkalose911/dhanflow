import React from "react";
import "../../Assests/CSS/Note.css";

const Note = () => {
  return (
    <p className="note">
      <strong>NOTE:</strong> First time using the product? Please download the file and extract it to the root directory of your C drive (C:).
    </p>
  );
};

export default Note;